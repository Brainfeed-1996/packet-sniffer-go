# Packet Sniffer Go

A lightweight network packet analyzer written in Go. Captures and inspects TCP/IP packets in real-time.

## Prerequisites
- Go 1.20+
- libpcap (Linux/Mac) or Npcap (Windows)

## Usage
```bash
go run main.go
```
